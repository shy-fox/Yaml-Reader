<span id='top'></span>
# Types in the SAML Library
> Everything below is subject to change, so don't rely on it
    
Inside this file you will find a list of available types for use inside the `SAML` library, each explaining how to use them.
- [`AbstractType<T>`](#abstracttype-sub-stylefont-size-small-source-code--sub)

## `AbstractType`<sub style='font-size: small'>[`source code ↗`](../src/main/java/io/shiromi/saml/types/AbstractType.java)</sub>
The backbone of each type, since it carries multiple methods which are sometimes overwritten by subclasses.

